int numPrint (int num, int length)
{
	int i, j, first, temp;
	char a;
	a <- 'x';
	jOut( "enter number");
	jIn (i);
	jOut(i);
	i <- length;
	while (i > 0)
	{
		first<- 0; /*this line contains a comment*/
		j <-1;
		while (j < i)
		{
			jOut( j);
			j <- j + 1 * 2 + 3;
			i <- first * 3 + 1;
			i <- first * (3 + 1);
			i <- (first + 3) * 1;
		}
		if (j < 2)
		{
			jOut(j);
		}
		else
		{
			jOut(i);
		}
		/* this is a comment */
		i<- i - 1;
		/*This is a
		Multiline
		Comment*/
	}
	jOut ("temp is ");
	jOut( temp);
	return i;
}