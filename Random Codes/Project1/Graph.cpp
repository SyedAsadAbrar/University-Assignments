#include<iostream>
#include<string>
#include"Graph.h"
using namespace std;
void main()
{
	////fin.getline(line, 50);
	//for (int i = 0; i < n; i++)
	//{
	//	//fin.getline(line, 50);
	//	int x = givesize(line);
	//	list[i] = new int[x];
	//	list[i] = putvalues(intarr[i], buff);
	//}

	Adjlist<int> al;
	al.Input();
	//string str = "0,1,0,2,0";
	//string str2 = "1,0,0";
	//cout << str.size();
	//cout << endl << str2.size();
	system("pause");
}